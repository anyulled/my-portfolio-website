# Use bounded response templates

Automatic Instagram responses will use predefined templates for the model-form and client-pricing routes. The AI may classify the conversation, detect its language, and select the route, but it will not compose unconstrained business text; this prevents accidental negotiation, commitments, or invented terms.
